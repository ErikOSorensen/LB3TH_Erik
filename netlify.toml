[build]
  command = "quarto render"
  publish = "_site"

[build.environment]
  # Quarto will be installed automatically by Netlify
  # No Python/R needed since we use freeze

[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
